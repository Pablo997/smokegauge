# Minimal sample config for local testing (smokegauge)
version: 1

defaults:
  timeout: 5s
  concurrency: 4

checks:
  - name: example health
    method: GET
    url: https://example.com/
    want_status: 200
